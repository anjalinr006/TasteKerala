import React from 'react';
import ReactDOM from 'react-dom';
import Base from './component/base';
import {Route,NavLink,Browse} from 'react-router-dom';


ReactDOM.render(
  <Base />,
  document.getElementById('root')
);
